package org.example.fineasy.models;

import org.example.fineasy.service.SortingService;

import java.util.Comparator;
import java.util.List;

// TODO: sorting method implementation, cannot use the build-in sort method from Comparable, define our own sort strategy
public class SortingServiceImp implements SortingService {
}
